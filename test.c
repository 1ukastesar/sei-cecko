#include<stdio.h>
#include<stdlib.h>

int stg(array[r][c]);

int main() {

    system("PAUSE");
}

int stg(a[r][c]) {
    for(i = 0; i < n; i++) {
        for(j = 0; j < n; j++) {
            printf("%i", a[i][j]);
        }
        printf("\n");
    }
}